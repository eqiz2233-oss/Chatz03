import type { Conversation, Order } from '../types';

/** Empty until wired to your API — inbox/orders merge from backend when available. */
export const conversations: Conversation[] = [];

export const orders: Order[] = [];
